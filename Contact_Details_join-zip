SELECT *
  --MARKET_NM
  --CALLDATE, POINTOFCONTACTNAME, SKILLNAME, TEAMNAME, ABANDONED, 
  --COUNT (DISTINCT CONTACTID) AS CALLS
FROM EDW_PROD.EDW_METRICS.NICE_CONTACT_DETAILS_V

JOIN EDW_PROD.EDW.ZIP_BY_MARKET_T2 zt2 ON zt2.ZIP_CODE = LEFT(SERVICE_ZIPCODE, 5)--SERVICE_ZIPCODE
    --AND zt2.meta_curr_rec_ind = 'Y'
    --AND zt2.meta_change_action_cd <> 'D'


WHERE CALLDATE >'2023-3-1' AND MEDIATYPENAME = 'Call'
  AND TYPE_DESC = 'Text_Desc'
  AND ACCOUNT_STATUS = 'Active'
  --AND POINTOFCONTACTNAME IN ('Chat')
  --AND SKILLNAME IN ('Biller_Chat')
  --GROUP BY 1,2,3,4,5 
  --ORDER BY CALLDATE
